
[build] publish = "." command = "" # 1. Sitemap Engine [[edge_functions]] function = "sitemap" path = "/sitemap.xml" # 2. Profile Engine (Bot Rendering) [[edge_functions]] function = "render-bot" path = "/sideline/*" [[edge_functions]] function = "render-bot" path = "/profile/*" # 3. Location Engine (Bot Rendering) [[edge_functions]] function = "ssr-province" path = "/location/*" [[edge_functions]] function = "ssr-province" path = "/province/*" # Security & Cache Headers [[headers]] for = "/*" [headers.values] X-Content-Type-Options = "nosniff" X-Frame-Options = "SAMEORIGIN" # SPA Fallback (สำคัญ: ต้องอยู่ล่างสุด) [[redirects]] from = "/*" to = "/index.html" status = 200

