


 # --- 1. การตั้งค่าการ Build --- [build] publish = "." command = "" # --- 2. Edge Functions (เรียงตามการใช้งาน) --- # Sitemap Engine [[edge_functions]] function = "sitemap" path = "/sitemap.xml" # Profile Engine (Bot Rendering) [[edge_functions]] function = "render-bot" path = "/sideline/*" [[edge_functions]] function = "render-bot" path = "/profile/*" # Location Engine (Bot Rendering) [[edge_functions]] function = "ssr-province" path = "/location/*" [[edge_functions]] function = "ssr-province" path = "/province/*" # --- 3. Security & Cache Headers --- [[headers]] for = "/*" [headers.values] X-Content-Type-Options = "nosniff" X-Frame-Options = "SAMEORIGIN" Cache-Control = "public, max-age=0, must-revalidate" # --- 4. Redirects (SPA Fallback) --- # สำคัญ: ต้องอยู่ล่างสุดเสมอ เพื่อไม่ให้ไปทับซ้อนกับ Edge Functions หรือไฟล์ที่มีอยู่จริง [[redirects]] from = "/*" to = "/index.html" status = 200 


