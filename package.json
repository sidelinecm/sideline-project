{
  "name": "sideline-project",
  "version": "1.0.0",
  "description": "A modern web project built with Tailwind CSS.",
  "private": true,
  "scripts": {
    "dev": "tailwindcss -i ./styles.css -o ./dist/output.css --watch",
    "clean": "rimraf ./dist",
    "build:css": "tailwindcss -i ./styles.css -o ./dist/output.css --minify",
    "build:copy": "copyfiles ./*.html main.js fonts/**/* images/**/* _headers _redirects netlify.toml dist/",
    "build": "npm run clean && mkdirp ./dist && npm run build:css && npm run build:copy"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "mkdirp": "^3.0.1",
    "rimraf": "^5.0.5",
    "tailwindcss": "^3.4.1"
  }
}