{
  "name": "sideline-project",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i ./styles.css -o ./dist/output.css --minify && cpx \"./**/*.{html,js,webp,png,svg,ico,woff2,xml}\" ./dist"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "tailwindcss": "^3.4.1"
  }
}